# Managed by Ansible — do not edit manually.
# {{ ansible_managed }}

# ── Domain ────────────────────────────────────────────────────────────
INFRAGUARD_DOMAIN={{ infraguard_domain }}
INFRAGUARD_DOMAIN_EMAIL={{ infraguard_domain_email }}

# ── TLS ───────────────────────────────────────────────────────────────
INFRAGUARD_LETSENCRYPT={{ infraguard_letsencrypt | lower }}
INFRAGUARD_TLS_CERT={{ infraguard_tls_cert }}
INFRAGUARD_TLS_KEY={{ infraguard_tls_key }}

# ── Dashboard API ─────────────────────────────────────────────────────
INFRAGUARD_API_TOKEN={{ infraguard_api_token }}

# ── Command Post ─────────────────────────────────────────────────────
COMMAND_POST_TOKEN={{ infraguard_command_post_token }}

# ── Upstream Teamservers ──────────────────────────────────────────────
INFRAGUARD_CS_UPSTREAM={{ infraguard_cs_upstream }}
INFRAGUARD_MYTHIC_UPSTREAM={{ infraguard_mythic_upstream }}
INFRAGUARD_BRC4_UPSTREAM={{ infraguard_brc4_upstream }}
INFRAGUARD_SLIVER_UPSTREAM={{ infraguard_sliver_upstream }}
INFRAGUARD_HAVOC_UPSTREAM={{ infraguard_havoc_upstream }}
INFRAGUARD_NIGHTHAWK_UPSTREAM={{ infraguard_nighthawk_upstream }}
INFRAGUARD_POSHC2_UPSTREAM={{ infraguard_poshc2_upstream }}

# ── GeoIP ─────────────────────────────────────────────────────────────
INFRAGUARD_GEOIP_DB={{ infraguard_geoip_db }}
INFRAGUARD_GEOIP_ASN_DB={{ infraguard_geoip_asn_db }}
INFRAGUARD_GEOIP_COUNTRY_DB={{ infraguard_geoip_country_db }}

# ── Filter / Pipeline ────────────────────────────────────────────────
INFRAGUARD_FILTER_MODE={{ infraguard_filter_mode }}

# ── Database ──────────────────────────────────────────────────────────
INFRAGUARD_DB_PATH={{ infraguard_db_path }}

# ── OPSEC ─────────────────────────────────────────────────────────────
INFRAGUARD_HEALTH_PATH={{ infraguard_health_path }}
INFRAGUARD_LOG_LEVEL={{ infraguard_log_level }}

# ── Decoy Pages ───────────────────────────────────────────────────────
IG_DECOY_PAGES_DIR={{ infraguard_decoy_pages_dir }}
IG_DECOY_SITE={{ infraguard_decoy_site }}

# ── Rules / Blocklists ────────────────────────────────────────────────
INFRAGUARD_RULES_DIR={{ infraguard_rules_dir }}
INFRAGUARD_BANNED_IP_FILE={{ infraguard_banned_ip_file }}

# ── Content Delivery ─────────────────────────────────────────────────
PWNDROP_TOKEN={{ infraguard_pwndrop_token }}
MYTHIC_URL={{ infraguard_mythic_url }}
MYTHIC_STAGE2_FILE_ID={{ infraguard_mythic_stage2_file_id }}

# ── Campaign Tokens ──────────────────────────────────────────────────
CAMPAIGN_TOKEN_Q1={{ infraguard_campaign_token_q1 }}
CAMPAIGN_TOKEN_Q2={{ infraguard_campaign_token_q2 }}
CAMPAIGN_HMAC_SECRET={{ infraguard_campaign_hmac_secret }}

# ── Burn Detection ───────────────────────────────────────────────────
GOOGLE_SAFEBROWSING_API_KEY={{ infraguard_google_safebrowsing_api_key }}

# ── SIEM Plugins ─────────────────────────────────────────────────────
ELASTICSEARCH_URL={{ infraguard_elasticsearch_url }}
ELASTICSEARCH_API_KEY={{ infraguard_elasticsearch_api_key }}
WAZUH_API_URL={{ infraguard_wazuh_api_url }}
WAZUH_INDEXER_URL={{ infraguard_wazuh_indexer_url }}
WAZUH_PASSWORD={{ infraguard_wazuh_password }}

# ── Webhook Plugins ──────────────────────────────────────────────────
DISCORD_WEBHOOK_URL={{ infraguard_discord_webhook_url }}
SLACK_WEBHOOK_URL={{ infraguard_slack_webhook_url }}
WEBHOOK_URL={{ infraguard_webhook_url }}
WEBHOOK_TOKEN={{ infraguard_webhook_token }}
