{
  "name": "infraguard-web",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "InfraGuard dashboard UI (Vite build).",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir static/dist --emptyOutDir",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
